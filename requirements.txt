# Minimal, practical requirements for running this project.
# For reproducible binary packages (numpy/scikit-learn) prefer using conda.

# Core web framework
Flask>=2.2,<3
Jinja2>=3.0,<4
Werkzeug>=2.2,<3
itsdangerous>=2.1,<3
click>=8.0,<9

# Data & ML
numpy>=1.24,<2
pandas>=2.0,<3
scikit-learn>=1.1,<2
joblib>=1.1,<2

# Networking & capture (optional runtime dependency)
pyshark>=0.4.2
# NOTE: pyshark requires the externally-installed 'tshark' (Wireshark CLI). Install via
#   - Debian/Ubuntu: sudo apt-get install -y tshark
#   - Windows: install Wireshark and enable tshark on PATH

# Utilities / testing
requests>=2.28,<3
pytest>=7.0

# Optional: production WSGI
gunicorn>=20.1

# If you want strict reproducibility, replace ranges with pinned versions or
# create a conda `environment.yml` that pins binaries for your OS.
